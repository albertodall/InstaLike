@using InstaLike.Web.Models

@model IEnumerable<PostModel>

@{
    ViewBag.Title = "Timeline";
}

<div class="posts">
    @foreach (var post in Model)
    {
        <partial name="~/Views/Post/_ViewPostPartial.cshtml" model="post" />
    }
</div>